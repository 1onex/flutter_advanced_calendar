# TileMatch3D offline runtime build request

This temporary PR exists only to run the verified dependency packaging workflow for the original React/Three.js game.

Revision 2 additionally vendors the exact `forest_slope_1k.hdr` used by the original Sakura scene so runtime lighting no longer depends on external network access.
